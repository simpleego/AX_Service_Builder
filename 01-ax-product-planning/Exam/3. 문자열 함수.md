# Python 문자열 함수·메서드 연습문제 42문제

## 1. `count()` — 문자 또는 문자열 개수 세기

### 문제 1

다음 문자열에서 문자 `"a"`가 몇 번 등장하는지 출력하세요.

```python
text = "banana"
```

예상 결과:

```text
3
```

### 문제 2

사용자로부터 이메일 주소를 입력받고, `"@"` 문자가 몇 개 포함되어 있는지 출력하세요.

```text
이메일 입력: python@gmail.com
@ 개수: 1
```

### 문제 3

다음 문장에서 `"Python"`이라는 단어가 몇 번 등장하는지 출력하세요.

```python
text = "Python is easy. I like Python. Python is powerful."
```

---

# 2. `find()` — 문자열 위치 찾기

`find()`는 찾지 못하면 `-1`을 반환합니다.

### 문제 4

다음 이메일 주소에서 `"@"`가 몇 번째 위치에 있는지 출력하세요.

```python
email = "student@gmail.com"
```

### 문제 5

사용자로부터 상품 코드를 입력받고 첫 번째 `"-"`의 위치를 출력하세요.

```text
상품코드: SAM-2026-001
```

### 문제 6

다음 파일명에서 `"."`의 위치를 출력하세요.

```python
filename = "report.pdf"
```

---

# 3. `index()` — 문자열 위치 찾기

`index()`는 `find()`와 비슷하지만 찾는 문자가 없으면 오류가 발생합니다.

### 문제 7

다음 문자열에서 `"@"`의 위치를 출력하세요.

```python
email = "python@naver.com"
```

### 문제 8

다음 학번에서 `"-"`의 위치를 출력하세요.

```python
student_id = "2026-CS-001"
```

### 문제 9

다음 URL에서 `":"`의 위치를 출력하세요.

```python
url = "https://www.python.org"
```

---

# 4. `join()` — 문자열 사이에 문자 삽입하기

`join()`은 보통 리스트와 함께 많이 사용합니다. 아직 리스트를 배우지 않았으므로 문자열의 **각 문자 사이에 다른 문자를 삽입하는 기능**만 연습합니다.

### 문제 10

다음 문자열의 각 문자 사이에 `"-"`를 넣어 출력하세요.

```python
code = "ABC"
```

결과:

```text
A-B-C
```

### 문제 11

다음 연도를 `"."`으로 연결하여 출력하세요.

```python
year = "2026"
```

결과:

```text
2.0.2.6
```

### 문제 12

사용자로부터 영문 이니셜을 입력받아 각 문자 사이에 `"★"`를 넣으세요.

```text
이니셜: KIM
결과: K★I★M
```

---

# 5. `upper()` — 대문자로 변환하기

### 문제 13

다음 문자열을 모두 대문자로 변환하세요.

```python
language = "python"
```

### 문제 14

사용자로부터 영문 이름을 입력받아 대문자로 출력하세요.

```text
이름: hong
결과: HONG
```

### 문제 15

다음 상품코드를 모두 대문자로 변환하세요.

```python
code = "abc-2026-ai"
```

---

# 6. `lower()` — 소문자로 변환하기

### 문제 16

다음 문자열을 모두 소문자로 변환하세요.

```python
language = "PYTHON"
```

### 문제 17

사용자로부터 이메일 주소를 입력받아 모두 소문자로 출력하세요.

```text
이메일: STUDENT@GMAIL.COM
결과: student@gmail.com
```

### 문제 18

다음 파일명을 모두 소문자로 변환하세요.

```python
filename = "REPORT.PDF"
```

---

# 7. `lstrip()` — 왼쪽 공백 제거하기

### 문제 19

다음 문자열의 왼쪽 공백을 제거하세요.

```python
name = "     홍길동"
```

### 문제 20

사용자로부터 입력받은 문자열의 왼쪽 공백을 제거해서 출력하세요.

```text
입력:      Python
결과: Python
```

### 문제 21

다음 상품명의 왼쪽 공백을 제거하세요.

```python
product = "       노트북"
```

---

# 8. `rstrip()` — 오른쪽 공백 제거하기

### 문제 22

다음 문자열의 오른쪽 공백을 제거하세요.

```python
name = "홍길동       "
```

### 문제 23

다음 상품명의 오른쪽 공백을 제거하세요.

```python
product = "무선 마우스       "
```

### 문제 24

사용자로부터 문자열을 입력받고 오른쪽 공백을 제거해서 출력하세요.

---

# 9. `strip()` — 양쪽 공백 제거하기

### 문제 25

다음 문자열의 양쪽 공백을 제거하세요.

```python
name = "     홍길동     "
```

### 문제 26

사용자로부터 아이디를 입력받고 양쪽 공백을 제거하세요.

```text
아이디:       python123
결과: python123
```

### 문제 27

다음 상품명의 양쪽 공백을 제거하세요.

```python
product = "       Galaxy Phone       "
```

---

# 10. `replace()` — 문자열 바꾸기

### 문제 28

다음 전화번호에서 `"-"`를 모두 제거하세요.

```python
phone = "010-1234-5678"
```

결과:

```text
01012345678
```

### 문제 29

다음 날짜의 `"-"`를 `"."`으로 변경하세요.

```python
date = "2026-09-11"
```

결과:

```text
2026.09.11
```

### 문제 30

다음 문장에서 `"Java"`를 `"Python"`으로 변경하세요.

```python
text = "나는 Java를 공부하고 있습니다."
```

---

# 11. `split()` — 문자열 나누기

`split()`의 결과는 리스트입니다. 현재는 리스트의 내부 데이터를 꺼내지 않고 **분리된 결과 전체를 출력**합니다.

### 문제 31

다음 문자열을 공백을 기준으로 분리하세요.

```python
text = "Python Java JavaScript"
```

예상 결과:

```text
['Python', 'Java', 'JavaScript']
```

### 문제 32

다음 날짜를 `"-"`를 기준으로 분리하세요.

```python
date = "2026-09-11"
```

예상 결과:

```text
['2026', '09', '11']
```

### 문제 33

다음 이메일 주소를 `"@"`를 기준으로 분리하세요.

```python
email = "student@gmail.com"
```

예상 결과:

```text
['student', 'gmail.com']
```

---

# 12. `isalpha()` — 문자로만 구성되어 있는지 확인

결과는 `True` 또는 `False`입니다.

### 문제 34

다음 문자열이 문자로만 이루어져 있는지 확인하세요.

```python
name = "Hong"
```

### 문제 35

다음 문자열이 문자로만 이루어져 있는지 확인하세요.

```python
name = "Hong123"
```

### 문제 36

사용자로부터 이름을 입력받아 문자로만 구성되어 있는지 확인하세요.

```text
이름: 홍길동
결과: True
```

---

# 13. `isdigit()` — 숫자로만 구성되어 있는지 확인

### 문제 37

다음 학번이 숫자로만 구성되어 있는지 확인하세요.

```python
student_id = "20260123"
```

### 문제 38

다음 전화번호가 숫자로만 구성되어 있는지 확인하세요.

```python
phone = "010-1234-5678"
```

결과:

```text
False
```

### 문제 39

사용자로부터 나이를 입력받아 숫자로만 입력했는지 확인하세요.

```text
나이: 25
결과: True
```

---

# 14. `startswith()` / `endswith()` — 시작과 끝 확인하기

## `startswith()`

### 문제 40

다음 전화번호가 `"010"`으로 시작하는지 확인하세요.

```python
phone = "010-1234-5678"
```

### 문제 41

다음 인터넷 주소가 `"https"`로 시작하는지 확인하세요.

```python
url = "https://www.python.org"
```

## `endswith()`

### 문제 42

다음 파일명이 `".pdf"`로 끝나는지 확인하세요.

```python
filename = "report.pdf"
```

---
